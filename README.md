
  # Pre-Delinquency Intervention Engine

  This is a code bundle for Pre-Delinquency Intervention Engine. The original project is available at https://www.figma.com/design/3xO99rGChq89vhfQKoNTre/Pre-Delinquency-Intervention-Engine.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  